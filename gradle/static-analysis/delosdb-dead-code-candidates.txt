# DelosDB curated dead-code / stale-code candidates.
# Format: relative/path.java|SymbolName|Reason
#
# Keep this list intentionally small. A row is added only after a concrete
# deletion hypothesis exists; the Gradle report is evidence, not automatic
# deletion authority. ServiceLoader, reflection, public API, JPMS exports,
# durable formats, and test-only compatibility can keep code alive without
# ordinary production-source references.
#
# Phase 6 closeout resolved the previous candidates:
#   * MvccDurableIndexStore is a compatibility facade used by the durable-index
#     proof tests.
#   * MvccVacuum is the coordinator used by durable and concurrent vacuum tests.
# Neither is dead code, so no active candidate rows remain.
