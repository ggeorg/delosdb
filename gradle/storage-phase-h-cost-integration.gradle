// Storage Phase H cost integration verification.
// H1 introduces the costable table-access seam only.
// H2 maps MVCC provider stats into the native Derby cost path as a diagnostic-only result.
// Derby optimizer cost consumption remains deferred.

ext.delosdbStoragePhaseHCostIntegrationModuleClassDir = { String projectPath, String moduleName ->
    project(projectPath).layout.buildDirectory.dir("classes/modules/${moduleName}")
}

ext.delosdbStoragePhaseHCostIntegrationRuntimeJars = {
    def runtimeJarsValue = rootProject.ext.delosdbRuntimeJars
    if (runtimeJarsValue instanceof Closure) {
        return runtimeJarsValue.call()
    }
    files(runtimeJarsValue ?: [])
}

ext.delosdbStoragePhaseHCostIntegrationClasspath = { Object smokeClassesDir ->
    files(
            smokeClassesDir,
            delosdbStoragePhaseHCostIntegrationModuleClassDir(':delosdb-engine', 'org.apache.derby.engine'),
            delosdbStoragePhaseHCostIntegrationModuleClassDir(':delosdb-engine-kernel', 'io.github.ggeorg.delosdb.engine.kernel'),
            delosdbStoragePhaseHCostIntegrationModuleClassDir(':delosdb-commons', 'org.apache.derby.commons'),
            delosdbStoragePhaseHCostIntegrationModuleClassDir(':delosdb-osgi-stub', 'org.osgi.framework'),
            project(':delosdb-spi').layout.buildDirectory.dir('classes/spi'),
            rootProject.layout.buildDirectory.file('libs/delosdb-storage-mvcc.jar'),
            delosdbStoragePhaseHCostIntegrationRuntimeJars())
}

ext.delosdbStoragePhaseH1CostableTableAccessClassesDir =
        layout.buildDirectory.dir('classes/storage-phase-h1-costable-table-access-smoke')

tasks.register('compileStoragePhaseH1CostableTableAccessSmoke', JavaCompile) {
    dependsOn 'jars', 'processDerbyResources', ':delosdb-spi:compileDelosDbSpi', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Compiles the Phase H1 costable table-access smoke test.'
    source = files('dev/storage-phase-h1-costable-table-access-smoke/src/main/java', rootProject.ext.delosdbSmokeUtilsSourceDir)
    destinationDirectory = delosdbStoragePhaseH1CostableTableAccessClassesDir
    sourceCompatibility = javaRelease.toString()
    targetCompatibility = javaRelease.toString()
    options.release = javaRelease
    options.encoding = 'UTF-8'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH1CostableTableAccessClassesDir)
}

tasks.register('storagePhaseH1CostableTableAccessSmoke', JavaExec) {
    dependsOn 'verifyStoragePhaseGConsolidation', 'compileStoragePhaseH1CostableTableAccessSmoke', 'jars', 'processDerbyResources', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Runs the Phase H1 costable table-access smoke test.'
    mainClass = 'delosdb.smoke.StoragePhaseH1CostableTableAccessSmoke'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH1CostableTableAccessClassesDir)
}

tasks.register('verifyStoragePhaseH1CostableTableAccess') {
    group = 'verification'
    description = 'Verifies the native MVCC table access exposes provider-backed cost estimates without Derby optimizer integration.'
    dependsOn 'storagePhaseH1CostableTableAccessSmoke'
    doLast {
        println 'Verified Phase H1 costable table-access seam; next step is H2 MVCC cost mapping into the native cost path'
    }
}

ext.delosdbStoragePhaseH2MvccCostMappingClassesDir =
        layout.buildDirectory.dir('classes/storage-phase-h2-mvcc-cost-mapping-smoke')

tasks.register('compileStoragePhaseH2MvccCostMappingSmoke', JavaCompile) {
    dependsOn 'jars', 'processDerbyResources', ':delosdb-spi:compileDelosDbSpi', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Compiles the Phase H2 MVCC native cost mapping smoke test.'
    source = files('dev/storage-phase-h2-mvcc-cost-mapping-smoke/src/main/java', rootProject.ext.delosdbSmokeUtilsSourceDir)
    destinationDirectory = delosdbStoragePhaseH2MvccCostMappingClassesDir
    sourceCompatibility = javaRelease.toString()
    targetCompatibility = javaRelease.toString()
    options.release = javaRelease
    options.encoding = 'UTF-8'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH2MvccCostMappingClassesDir)
}

tasks.register('storagePhaseH2MvccCostMappingSmoke', JavaExec) {
    dependsOn 'verifyStoragePhaseH1CostableTableAccess', 'compileStoragePhaseH2MvccCostMappingSmoke', 'jars', 'processDerbyResources', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Runs the Phase H2 MVCC native cost mapping smoke test.'
    mainClass = 'delosdb.smoke.StoragePhaseH2MvccCostMappingSmoke'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH2MvccCostMappingClassesDir)
}

tasks.register('verifyStoragePhaseH2MvccCostMapping') {
    group = 'verification'
    description = 'Verifies MVCC provider stats are mapped into the native Derby cost path without optimizer consumption.'
    dependsOn 'storagePhaseH2MvccCostMappingSmoke'
    doLast {
        println 'Verified Phase H2 MVCC native cost mapping; next step is H3 heap proof-only cost mapping'
    }
}


ext.delosdbStoragePhaseH3HeapCostProofClassesDir =
        layout.buildDirectory.dir('classes/storage-phase-h3-heap-cost-proof-smoke')

tasks.register('compileStoragePhaseH3HeapCostProofSmoke', JavaCompile) {
    dependsOn 'jars', 'processDerbyResources', ':delosdb-spi:compileDelosDbSpi', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Compiles the Phase H3 heap proof-only cost mapping smoke test.'
    source = files('dev/storage-phase-h3-heap-cost-proof-smoke/src/main/java', rootProject.ext.delosdbSmokeUtilsSourceDir)
    destinationDirectory = delosdbStoragePhaseH3HeapCostProofClassesDir
    sourceCompatibility = javaRelease.toString()
    targetCompatibility = javaRelease.toString()
    options.release = javaRelease
    options.encoding = 'UTF-8'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH3HeapCostProofClassesDir)
}

tasks.register('storagePhaseH3HeapCostProofSmoke', JavaExec) {
    dependsOn 'verifyStoragePhaseH2MvccCostMapping', 'compileStoragePhaseH3HeapCostProofSmoke', 'jars', 'processDerbyResources', ':delosdb-storage-mvcc:delosDbStorageMvccJar'
    group = 'verification'
    description = 'Runs the Phase H3 heap proof-only cost mapping smoke test.'
    mainClass = 'delosdb.smoke.StoragePhaseH3HeapCostProofSmoke'
    classpath = delosdbStoragePhaseHCostIntegrationClasspath(delosdbStoragePhaseH3HeapCostProofClassesDir)
}

tasks.register('verifyStoragePhaseH3HeapCostProof') {
    group = 'verification'
    description = 'Verifies inherited Derby heap cost data maps through the proof-only heap table-access cost surface without optimizer consumption.'
    dependsOn 'storagePhaseH3HeapCostProofSmoke'
    doLast {
        println 'Verified Phase H3 heap proof-only cost mapping; next step is H4 session-tunable Derby cost constant'
    }
}
