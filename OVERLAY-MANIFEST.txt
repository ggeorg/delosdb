DelosDB stable-suite Gen2 registration overlay

Changed repository files:
- gradle/testing/delos-test-suite-registry.tsv
- gradle/testing/delosdb-test-suites.gradle

Purpose:
- Register the seven accepted DelosDB Gen2/cardinality/archived-undo correctness and recovery sources in the permanent stable-suite registry.
- Keep MutationPhysicalAmplificationLedgerTest classified as release-only performance evidence and include its existing focused task/class in the permanent performance aggregate/allow-list.

No production Java, test Java, benchmark SQL, or task implementation is changed.
