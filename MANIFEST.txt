DelosDB pre-full-benchmark cleanup overlay

Purpose
-------
Return the working tree to the accepted post-modernization production state
before running the full cross-engine benchmark checkpoint.

KEPT
----
- MVCC unique-index cardinality production fix.
- Statement-lifetime MVCC read boundaries for NOHOLD base fetches.
- MvccStatementReadBoundaryPolicyTest and stable-suite registration.
- Physical scan costing default.
- Physical RowLocation costing default.
- Consolidated materialized CURRENT decoder.
- Consolidated non-covering base fetch decoder.
- Statement-lifetime read boundaries.
- Canonical F04 simple-join statistics stabilization.
- Canonical F05 multi-join statistics stabilization.
- Existing permanent benchmark and attribution infrastructure from the
  accepted source authority.

REMOVED / RESTORED
------------------
- Failed bounded/page-grouped INDEX_TO_BASE_ROW batch prototype:
    AccessMethodBaseFetchBatch
    IndexRowToBaseRowResultSet batch buffering
    MVCC batch fetch implementation
    hinted-page batch decoder helper
- Temporary F05 row-production, covering-index, PK-suffix and covering-plan
  diagnostic workload/task additions.
- Phase-2D CLOSE_CURSORS_AT_COMMIT reachability modification that existed only
  to test the rejected batching prototype.

Deletion
--------
Run:
  sh cleanup-pre-full-benchmark.sh

The cleanup script removes the obsolete AccessMethodBaseFetchBatch.java file
and deletes itself.

No benchmark thresholds are changed.
No production performance promotion is reverted.
