DelosDB Gen2-C3 F04 NOHOLD stable-suite registration overlay

Purpose
-------
Register MvccStatementReadBoundaryPolicyTest in the permanent DelosDB stable
test-suite registry.

Registration
------------
suite: concurrency
execution tier: full

Reason
------
The test freezes the RawStore locking-policy capability contract that permits
statement-lifetime MVCC read boundaries for READ_COMMITTED_NOHOLDLOCK while
preserving immutable-page-read=false.

Scope
-----
Registry only. No production code, test implementation, benchmark behavior,
thresholds, or static-analysis baselines are changed.
