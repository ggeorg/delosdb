DelosDB Gen2-C3 F04 evidence-marker contract fix

Changed files
-------------
delosdb-tests/build.gradle

Purpose
-------
Validate the disposable-index-statistics diagnostic state from the server
container manifest, which is the evidence file that actually records it,
instead of incorrectly requiring the marker in the benchmark summary.

Behavior
--------
Harness-only. No production engine, optimizer, MVCC, RawStore, WAL, recovery,
durable-format, workload, cost-model, or benchmark-threshold changes.
