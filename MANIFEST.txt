F08-P RawStore log-append contention JFR diagnostic

Changed repository files:
- delosdb-tests/build.gradle
- delosdb-tests/src/delosTestSupport/java/io/github/ggeorg/delosdb/benchmark/jdbc/DelosJdbcCrossEngineConcurrency.java

Purpose:
- Run the validated 8-client, 16-byte PRIMARY_KEY_ONLY INSERT_100 server-JFR diagnostic with Derby-family physical WAL I/O minimized (1 MiB log buffer + durability=test).
- Derive a profile.jfc configuration with jdk.JavaMonitorEnter threshold=1 us so short monitor-enter stalls are visible.
- Keep production code unchanged.
