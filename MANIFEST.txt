DelosDB Gen2-C3 F05 rejected-experiment cleanup overlay

Purpose
-------
Remove stale Phase-2H diagnostic source that accumulated in the working tree
before testing the new Gen2-C3 batched INDEX_TO_BASE_ROW experiment.

Why this is required
--------------------
The batching overlay restores MvccRawStoreTable from the clean current C3
source, but MvccRawStoreRowDirectory was still carrying the rejected
mvccReadDirectoryHeadOnly experiment and referenced:
  MvccRawStoreTable.decodeDirectoryReadHead(...)

That method correctly no longer exists after restoring the clean table source,
causing compilation to fail.

Restored files
--------------
MvccRawStoreRowDirectory.java
  removes read-directory-head-only diagnostic

MvccRawStoreVersionReader.java
  removes rejected single-pass/page-run/lazy-metadata diagnostics

MvccRawStoreVersionRows.java
  removes rejected projected-payload/lazy-metadata diagnostics

BTreeScan.java
  removes rejected SearchParameters reuse diagnostic

Scope
-----
The new batched base-fetch experiment is NOT removed:
  AccessMethodBaseFetchBatch.java
  IndexRowToBaseRowResultSet.java
  MvccRawStoreConglomerateController.java
  MvccRawStoreTable.java

Production F04 cardinality and NOHOLD statement-boundary promotions are also
untouched.
